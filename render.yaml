services:
  - type: web
    name: generador-assumpta
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false # Render will prompt you to enter the value securely in the dashboard
